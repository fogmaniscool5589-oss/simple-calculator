# Simple Calculator

A simple calculator that multiplies two numbers.

## How to Use

1. Download the `calculator.py` file (click the "Raw" button and save it)
2. Open a terminal/command prompt in the folder where you saved it
3. Run the script:
   ```bash
   python calculator.py
